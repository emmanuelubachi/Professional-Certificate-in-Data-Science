# Solving the equation 2x^2 - x - 4 = 0

# assigning values to variables
a <- 2
b <- -1
c <- -4

# solving the quadratic equation
(-b + sqrt(b^2 - 4*a*c))/(2*a)
(-b - sqrt(b^2 - 4*a*c))/(2*a)


# log base 4 of 1024
log(1024, 4)

library(dslabs)
data(movielens)
str(movielens)

nlevels(movielens$genres)
levels(movielens$genres)
